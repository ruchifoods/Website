@{
    Layout = "~/Views/Shared/_LayoutCustomer.cshtml"; 
    ViewBag.Title = "Reset Link Sent";
}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header bg-success text-white">
                    <h3 class="mb-0">Check Your Email</h3>
                </div>
                <div class="card-body">
                    <p>
                        I
                        If an account with that email exists, we have sent a password reset link. Please check
                    </p>
                    <p class="text-muted">
                        If you don't receive an email within a few minutes, check your spam folder or try again
                    </p>
                    <div class="d-flex justify-content-end">
                        <a href="@Url.Action("Login", "CustomerAccount")" class="btn btn-primary">Back to Login
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>